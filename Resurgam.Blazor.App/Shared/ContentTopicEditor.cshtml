@using Resurgam.Infrastructure.ViewModels

    @if (Topic != null)
    {

        <textarea type="text" id="froalaEdit" name="topicContent" bind="@Topic.TopicContent" />
    }

    

@functions {
    [Parameter]
    protected TopicEditViewModel Topic { get; set; }

    public static void InitFroalaEditor(string elementId) => JSRuntime.Current.InvokeAsync<string>("InitFroalaEditor", elementId);

    protected override async Task OnAfterRenderAsync()
    {
        //await JSRuntime.Current.InvokeAsync<string>("InitFroalaEditor", "#froalaEdit");
    }
}


